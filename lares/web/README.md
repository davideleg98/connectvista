# Lares Web

Vite + React + TypeScript + MapLibre GL JS frontend for Lares. See
`../ARCHITECTURE.md` for why this stack was chosen over the founding spec's
suggested Next.js, and `../DEVELOPMENT.md` to run it.

```bash
npm install
cp .env.example .env
npm run dev -- --port 5183
```
